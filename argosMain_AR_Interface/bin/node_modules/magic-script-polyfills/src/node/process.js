import { fs, Fs } from 'uv';

var process = {
    platform: "linux",
    release: "mxs",
    cwd() {
        return fs.realpath(new Fs(), ".");
    },
    nextTick(fn) {
        Promise.resolve().then(fn);
    },
};

export default process;
